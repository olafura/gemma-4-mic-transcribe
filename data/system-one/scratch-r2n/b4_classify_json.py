# Hard negatives: classification instructions (30) and requests to output JSON (25).
ROWS = [
# --- classification ---
dict(cat="hn_classify", lang="en", sys="You are a ticket triage assistant. Answer with exactly one label: billing, technical, account, or other.",
     prompt="""My card was charged twice for the same month and the second charge shows a different amount."""),
dict(cat="hn_classify", lang="en", sys="Classify each message as billing, technical or other. One word only.",
     prompt="""The export button spins forever on Safari but works in Chrome."""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Label this review as positive, negative or mixed, and give one line of reasoning: 'Delivery was two days late and the box was battered, but the chair itself is lovely and easy to assemble.'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Is this email spam, a phishing attempt, or legitimate? 'Your package could not be delivered. Confirm your address within 24 hours at posturinn-delivery-check.info or it will be returned.'"""),
dict(cat="hn_classify", lang="en", sys="You tag support conversations with exactly one of: bug, feature_request, question, complaint.",
     prompt="""It would be great if the calendar view could also show cancelled bookings greyed out instead of hiding them."""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Sort these five expenses into travel, food, hardware or other: taxi 4200, USB-C hub 7990, team lunch 18400, domain renewal 2100, train ticket 6500."""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Which department should this go to — sales, support, or legal? 'We are a university and need to know whether your standard terms allow us to store student data in the EU only.'"""),
dict(cat="hn_classify", lang="en", sys="You are a content moderator. Reply with allow, review, or remove and a short reason.",
     prompt="""'This restaurant is run by crooks, the owner short-changed me twice and I will be waiting outside on Friday.'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Label the sentiment of each line as positive, neutral or negative: 1) 'Fast checkout.' 2) 'It arrived.' 3) 'Third time the app logged me out mid-form.'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Categorise this bug report by severity — blocker, major, minor, cosmetic — and say what evidence you used: 'Timestamps on the invoice PDF are one hour out for users in Portugal during summer.'"""),
dict(cat="hn_classify", lang="en", sys="You classify incoming CVs as strong_fit, possible, or no_fit for a backend role. Explain in one sentence.",
     prompt="""Six years of PHP and MySQL, has run a small team, no cloud experience, keen to learn Go."""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Which of these belong in the 'urgent' bucket and which can wait until Monday? A locked-out user, a typo on the pricing page, a failing nightly backup, a customer asking about roadmap."""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Tag this sentence for intent: booking, cancellation, complaint or enquiry. 'We had a table for four at seven but there will now be six of us, is that possible?'"""),
dict(cat="hn_classify", lang="en", sys="You label log lines as noise, warning or incident.",
     prompt="""'disk usage on /var at 91 percent, growing 2 percent per hour'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Is the following a fact, an opinion, or a prediction? 'The new harbour tunnel will cut morning journeys by a third once it opens.'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Given the categories fiction, memoir, history and self-help, where would you file a book that follows one family through three generations but insists everything in it happened?"""),
dict(cat="hn_classify", lang="en", sys="You are an email assistant. Label each message: action_needed, fyi, or archive.",
     prompt="""'Reminder: the office will be closed on the 17th. No action required.'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Classify this support message by language and by topic, and say how confident you are: 'Hae, tilkynningin um reikninginn kom tvisvar i dag.'"""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Put each of these into one of: hardware fault, software bug, user error. 1) laptop will not charge on one cable but will on another 2) file saved but not visible until refresh 3) printed in A4 instead of A5."""),
dict(cat="hn_classify", lang="en", sys=None,
     prompt="""Rate the reading level of this paragraph as primary, secondary or adult, and point at the features that decide it: 'The kettle boiled. Outside, the gulls went on with their argument, indifferent to the weather or to us.'"""),
dict(cat="hn_classify", lang="de", sys="Du ordnest Support-Tickets genau einer Kategorie zu: Abrechnung, Technik, Konto, Sonstiges.",
     prompt="""Die Rechnung fuer Februar ist doppelt abgebucht worden, und die Summe stimmt auch nicht."""),
dict(cat="hn_classify", lang="de", sys=None,
     prompt="""Ist diese Kundenbewertung positiv, negativ oder gemischt? 'Der Versand war schnell, aber die Anleitung war unbrauchbar und ein Teil hat gefehlt.'"""),
dict(cat="hn_classify", lang="fr", sys="Tu classes les messages entrants en: facturation, technique, autre. Reponds par un seul mot.",
     prompt="""Impossible de telecharger la facture depuis hier soir, le bouton ne fait rien."""),
dict(cat="hn_classify", lang="es", sys=None,
     prompt="""Clasifica este comentario como queja, sugerencia o pregunta, y justifica en una linea: 'Seria util poder pagar en dos plazos, aunque entiendo que complique la contabilidad.'"""),
dict(cat="hn_classify", lang="it", sys=None,
     prompt="""Etichetta questa segnalazione come bug, richiesta di funzionalita o domanda: 'Non riesco a capire se il report include anche gli ordini annullati.'"""),
dict(cat="hn_classify", lang="ja", sys="あなたは問い合わせを「請求」「技術」「その他」のいずれか一つに分類します。",
     prompt="""ログインは できるのですが、請求書のページだけ真っ白になります。"""),
dict(cat="hn_classify", lang="pt", sys=None,
     prompt="""Classifique esta mensagem como elogio, reclamacao ou duvida: 'O atendimento foi rapido, mas ainda nao entendi porque o valor mudou.'"""),
dict(cat="hn_classify", lang="nl", sys=None,
     prompt="""Deel deze meldingen in als storing, vraag of wens: 1) de app crasht bij het openen 2) kan ik twee accounts koppelen 3) graag een donkere modus."""),
dict(cat="hn_classify", lang="ko", sys=None,
     prompt="""다음 리뷰를 긍정, 부정, 중립 중 하나로 분류하고 이유를 한 문장으로 적어 주세요. '배송은 빨랐지만 포장이 엉망이었습니다.'"""),
dict(cat="hn_classify", lang="zh", sys="你是工单分类助手，只回答一个标签：账单、技术、账号、其他。",
     prompt="""我的发票上公司名称写错了，需要重新开一张。"""),

# --- requests to output JSON ---
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Turn this into JSON with keys name, street, postcode and city: 'Anna Petursdottir, Laugavegur 44, 101 Reykjavik'. Output only the JSON."""),
dict(cat="hn_json_output", lang="en", sys="Always answer with valid JSON and nothing else.",
     prompt="""List the three largest cities in Portugal with their approximate populations."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Give me a JSON array of objects with fields step, tool and minutes for changing a bicycle inner tube. Six steps is plenty."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Extract the meeting details from this note as JSON (title, date, time, attendees): 'Design sync moved to Tue 14 Oct, 10:30, with Bjorn, Hulda and me.'"""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Write a JSON Schema for a product with id (uuid), title, price in minor units, and an optional list of tags."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Convert this YAML to JSON and keep the ordering:
server:
  host: localhost
  port: 8080
  tls: false"""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Produce a JSON object summarising this sentence with keys subject, action and object: 'The council approved the cycle lane after two years of consultation.'"""),
dict(cat="hn_json_output", lang="en", sys="You return only compact JSON. No prose, no code fences.",
     prompt="""Give me the ISO codes and native names for the Nordic languages."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Parse this receipt line by line into JSON with item, qty and price: 'coffee beans 1 2490 / oat milk 2 398 / filter papers 1 690'."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Write an example JSON payload for a webhook that reports a failed payment, including a nested customer object and a reason code."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Give me a JSON lookup table mapping the twelve months to their number of days in a leap year."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Rewrite this shopping list as JSON grouped by aisle: bread, milk, tinned tomatoes, apples, washing up liquid, cheddar, pasta."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Read this paragraph and return JSON with the fields company, role, years and location: 'Before joining us, Sara spent four years at a logistics firm in Rotterdam running their warehouse software.'"""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Produce a small JSON fixture with five fake users: id, name, email, created_at in ISO 8601, and an active boolean."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Convert this CSV row into a JSON object using the header line as keys:
name,age,city
Tomas,41,Porto"""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Give me the same recipe ingredients as JSON, with quantities normalised to grams where you can: 2 cups flour, 1 tsp salt, 7 g yeast, 350 ml water."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Write a JSON object describing the opening hours of a shop that is closed Sundays, open 9 to 18 on weekdays and 10 to 15 on Saturday."""),
dict(cat="hn_json_output", lang="en", sys=None,
     prompt="""Turn these bullet notes into JSON with keys topic, owner and due: 'pricing page copy — Hulda — Friday; migration dry run — me — next Wednesday'."""),
dict(cat="hn_json_output", lang="de", sys=None,
     prompt="""Wandle diese Adresse in JSON um, mit den Feldern name, strasse, plz und ort: 'Martin Kleiber, Gartenweg 7, 30173 Hannover'. Nur das JSON bitte."""),
dict(cat="hn_json_output", lang="fr", sys="Reponds uniquement en JSON valide.",
     prompt="""Donne les cinq plus grands lacs d'Europe avec leur superficie approximative."""),
dict(cat="hn_json_output", lang="es", sys=None,
     prompt="""Convierte esta nota en JSON con las claves titulo, fecha y participantes: 'Revision de presupuesto el jueves 12 a las 9, con Marta y Luis'."""),
dict(cat="hn_json_output", lang="ja", sys=None,
     prompt="""次のメモをJSONにしてください。キーは件名、日付、担当者です。「在庫の棚卸しは十月三日、担当は佐藤さん」"""),
dict(cat="hn_json_output", lang="pt", sys=None,
     prompt="""Transforme esta lista em JSON agrupado por categoria: arroz, feijao, sabao, detergente, laranja, banana."""),
dict(cat="hn_json_output", lang="nl", sys=None,
     prompt="""Geef me een JSON-object met de openingstijden van een bibliotheek die op maandag dicht is en verder van 10 tot 17 open."""),
dict(cat="hn_json_output", lang="zh", sys=None,
     prompt="""请把这段话整理成 JSON，字段为 姓名、职位、入职年份：「李敏，产品经理，2021 年加入」。只输出 JSON。"""),
]
