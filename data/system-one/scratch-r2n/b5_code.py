# General: coding (70 rows), many languages.
ROWS = [
dict(cat="code", lang="en", sys=None, prompt="""Write a Go function that reads a file line by line and returns the ten longest lines, without loading the whole file into memory."""),
dict(cat="code", lang="en", sys=None, prompt="""In Elixir, how would you supervise a GenServer that must restart with the same state it had before it crashed? Sketch the module."""),
dict(cat="code", lang="en", sys="You are a pragmatic staff engineer. Prefer boring solutions and say what you are trading away.", prompt="""We need idempotent HTTP POST handlers in a Phoenix app. What is the simplest thing that works across two nodes?"""),
dict(cat="code", lang="en", sys=None, prompt="""Show me a Rust iterator chain that takes a slice of structs and produces a HashMap grouping them by a string field."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a SQL query that finds customers who ordered in January but not in February, using a single scan of the orders table if you can."""),
dict(cat="code", lang="en", sys=None, prompt="""What is the difference between a defer in Go and a try/finally in Java, particularly around when the value of a named return is decided?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a small TypeScript type that makes exactly one of two fields required, and show how it errors when both are given."""),
dict(cat="code", lang="en", sys=None, prompt="""Implement binary search in C that returns the insertion point when the value is absent. Mention the overflow trap in the midpoint."""),
dict(cat="code", lang="en", sys=None, prompt="""How do I write a Python context manager that times the block and logs the duration, using contextlib?"""),
dict(cat="code", lang="en", sys=None, prompt="""In Kotlin, when should I use a sealed interface rather than an enum class? Give a small example where the enum falls short."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a bash script that finds all files over 100 MB under the current directory, sorts them by size and prints human readable sizes."""),
dict(cat="code", lang="en", sys=None, prompt="""Show an idiomatic Ruby way to memoise an expensive method on an instance, including the nil-result pitfall."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a CSS grid layout for a three column dashboard that collapses to one column under 700 pixels, without media query duplication if possible."""),
dict(cat="code", lang="en", sys=None, prompt="""In Java, what actually happens when two threads call computeIfAbsent on the same ConcurrentHashMap key with an expensive function?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a Swift extension on Array that chunks it into slices of n, and explain why you would return ArraySlice rather than Array."""),
dict(cat="code", lang="en", sys="You review code for a small team. Be direct about what you would change and why.", prompt="""Is it reasonable to put business logic in Postgres triggers if the same data is written by two services?"""),
dict(cat="code", lang="en", sys=None, prompt="""Show me how to write a property based test in Python with Hypothesis for a function that round-trips a string through encode and decode."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a PHP function that validates an IBAN with the mod-97 check, and say what it does not verify."""),
dict(cat="code", lang="en", sys=None, prompt="""How does React's useEffect cleanup interact with strict mode double invocation in development? Give a subscription example."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a small Lua script for Redis that atomically decrements a counter but never below zero, and returns the resulting value."""),
dict(cat="code", lang="en", sys=None, prompt="""In C#, what is the difference between IEnumerable and IAsyncEnumerable for a database read, and when does deferred execution bite?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write an awk one-liner that sums the fourth column of a tab separated file, skipping the header line."""),
dict(cat="code", lang="en", sys=None, prompt="""Sketch an Erlang process that batches incoming messages and flushes every 200 ms or 100 messages, whichever comes first."""),
dict(cat="code", lang="en", sys=None, prompt="""How do I structure a Django project so that a management command and a view can share the same business logic without importing views?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a regex that matches Icelandic postcodes (three digits, 101 to 902) and explain why a bare \\d{3} is not enough."""),
dict(cat="code", lang="en", sys=None, prompt="""Show a Haskell function that parses a list of key=value strings into a Map, handling duplicate keys by keeping the last one."""),
dict(cat="code", lang="en", sys=None, prompt="""What is the cleanest way in Node to stream a large JSON array from disk and process it object by object?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a Postgres query using a window function to find, per user, the gap in days between consecutive logins."""),
dict(cat="code", lang="en", sys="You explain code as if pairing: short sentences, concrete names.", prompt="""Walk me through writing a debounce function in plain JavaScript, including the trailing call and cancellation."""),
dict(cat="code", lang="en", sys=None, prompt="""In Terraform, how do I move a resource into a module without destroying it? Show the command and the risk."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a small Zig function that reads a u32 from a byte slice in little endian, with bounds checking."""),
dict(cat="code", lang="en", sys=None, prompt="""What is the idiomatic Scala way to turn a List of Either into an Either of List, and what do I lose by short-circuiting?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a GitHub Actions workflow that runs tests on three Node versions and only publishes on a tag."""),
dict(cat="code", lang="en", sys=None, prompt="""Explain how to paginate a REST API by cursor rather than offset, and what the cursor should encode."""),
dict(cat="code", lang="en", sys=None, prompt="""Write a Python dataclass for a money amount in minor units with a currency, and implement addition that refuses mixed currencies."""),
dict(cat="code", lang="en", sys=None, prompt="""How would you implement optimistic locking in a REST update endpoint, using ETags rather than a version column?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a Vim substitution that converts snake_case identifiers to camelCase on the current line only."""),
dict(cat="code", lang="en", sys=None, prompt="""Show a minimal WebSocket server in Elixir with Phoenix Channels that broadcasts a counter to all subscribers once a second."""),
dict(cat="code", lang="en", sys=None, prompt="""What is the right way to seed randomness in a Go test so failures are reproducible but runs still differ?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a shell function that opens the most recently modified file in a directory tree in $EDITOR, ignoring .git."""),
dict(cat="code", lang="en", sys=None, prompt="""Explain the difference between a left join and a lateral join in Postgres, with a query where only the lateral version works."""),
dict(cat="code", lang="en", sys=None, prompt="""How do I make a Python package that exposes a CLI entry point with pyproject.toml and no setup.py?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a small Prolog program that solves the classic three jugs water measuring puzzle, and explain the state representation."""),
dict(cat="code", lang="en", sys=None, prompt="""In SwiftUI, why does my list flicker when the identifiable id changes, and what is the usual fix?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a Nix expression for a dev shell with Erlang, Elixir and ffmpeg, pinned to a specific nixpkgs revision."""),
dict(cat="code", lang="en", sys=None, prompt="""Show how to test a function that reads the system clock, without freezing time globally across the test suite."""),
dict(cat="code", lang="en", sys=None, prompt="""What is the practical difference between UNIQUE and a unique index in Postgres, and does it matter for upsert?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a small C++ RAII wrapper for a file descriptor with move semantics and no copy."""),
dict(cat="code", lang="en", sys=None, prompt="""How would you shard a background job queue by tenant so one noisy tenant cannot starve the rest?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write an SQL migration that adds a not-null column with a default to a 200 million row table without a long lock. Which Postgres version matters here?"""),
dict(cat="code", lang="en", sys=None, prompt="""In pandas, what is the difference between .loc with a boolean mask and .query, in terms of copies and chained assignment warnings?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write a small state machine in TypeScript for an upload that can be idle, uploading, failed or done, using a discriminated union."""),
dict(cat="code", lang="en", sys=None, prompt="""Explain how to make a Docker image for a Rust binary under 20 MB, and where the size usually goes."""),
dict(cat="code", lang="en", sys=None, prompt="""How do I compare two floating point numbers in a test without either exact equality or an arbitrary epsilon?"""),
dict(cat="code", lang="en", sys=None, prompt="""Write an Elixir function that streams a CSV file, validates each row against a struct, and collects errors with their line numbers."""),
dict(cat="code", lang="de", sys=None, prompt="""Wie implementiere ich in Java ein einfaches LRU-Cache mit LinkedHashMap? Zeig mir die Ueberschreibung von removeEldestEntry."""),
dict(cat="code", lang="de", sys=None, prompt="""Was ist in Python der Unterschied zwischen einem Generator und einem Iterator, und wann brauche ich wirklich yield from?"""),
dict(cat="code", lang="fr", sys=None, prompt="""Comment ecrire en Rust une fonction qui renvoie un iterateur sans allouer de Vec? Montrez la signature avec impl Iterator."""),
dict(cat="code", lang="fr", sys=None, prompt="""Explique la difference entre git merge --squash et git rebase -i pour nettoyer une branche avant une revue."""),
dict(cat="code", lang="es", sys=None, prompt="""Escribe una consulta SQL que calcule el total mensual de ventas y el acumulado del ano, usando una funcion de ventana."""),
dict(cat="code", lang="es", sys=None, prompt="""Como evito condiciones de carrera al incrementar un contador en Redis desde varios procesos? Explica INCR y las alternativas."""),
dict(cat="code", lang="it", sys=None, prompt="""Come si gestiscono le migrazioni del database in un progetto con piu sviluppatori senza conflitti continui?"""),
dict(cat="code", lang="ja", sys=None, prompt="""PythonでJSONを読み込むときに、キーが欠けている場合の安全な書き方を教えてください。例も欲しいです。"""),
dict(cat="code", lang="ja", sys=None, prompt="""TypeScriptのunknownとanyの違いを、実際にバグを防いだ例で説明してください。"""),
dict(cat="code", lang="pt", sys=None, prompt="""Como faco paginacao eficiente em Postgres para uma tabela grande, sem usar OFFSET? Mostre um exemplo com chave composta."""),
dict(cat="code", lang="nl", sys=None, prompt="""Hoe schrijf ik in Go een test die een HTTP-handler test zonder een echte server te starten?"""),
dict(cat="code", lang="ko", sys=None, prompt="""파이썬에서 asyncio로 동시에 100개의 HTTP 요청을 보내되, 동시 실행 수를 10개로 제한하려면 어떻게 하나요?"""),
dict(cat="code", lang="zh", sys=None, prompt="""在 Go 里怎样优雅地关闭一个正在处理请求的 HTTP 服务？请给出示例代码。"""),
dict(cat="code", lang="is", sys=None, prompt="""Hvernig skrifa eg fall i Elixir sem les JSON skra og skilar {:ok, gogn} eda {:error, astaeda}? Syndu daemi med pattern matching."""),
dict(cat="code", lang="is", sys=None, prompt="""Hver er munurinn a map og flat_map i Elixir, og hvenaer borgar sig ad nota Stream i stadinn?"""),
]
