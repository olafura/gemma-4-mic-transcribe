# Hard negatives: "should I X or Y" advice (40) and tool/function-calling system prompts with JSON schemas (30).
ROWS = [
# --- should I X or Y ---
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I take the train and lose four hours, or fly and deal with the airport twice? It is a two day trip and I can work on the train."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I fix the failing integration test properly or delete it and write a smaller one? It has been skipped for three months."""),
dict(cat="hn_should_i", lang="en", sys="You give straight advice and say when the question is really about something else.", prompt="""Should I tell my manager I am interviewing elsewhere, or keep it quiet until I have an offer?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we cache the rendered page or the underlying query results? Traffic is spiky and the data changes maybe twice an hour."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I buy the slightly too small flat now or wait a year and hope for something better in the same street?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I learn Rust or get much better at the Python I already use daily? I write data pipelines."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should the migration run as part of the deploy or as a separate step a human triggers? We deploy about five times a day."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I repot this fig now in October or wait until spring? It is clearly root bound but it is also sulking."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we hire a second backend engineer or a proper QA person? Three engineers, lots of regressions, deadlines slipping."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I send the long email explaining everything, or ask for fifteen minutes on a call? The topic is a missed deadline that was partly my fault."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I replace the boiler now while the plumber is here for something else, or get two more quotes first?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we split this service in two or just clean up the module boundaries inside it? Team of four, one deploy pipeline."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I use my holiday for a long trip in summer or spread it over long weekends? I burn out around week seven."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should the retry live in the client library or in the calling service? I keep going back and forth."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I finish this novel I am not enjoying at page 200, or give myself permission to stop?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we run the workshop in person and lose the remote half, or online and lose the energy? Fourteen people, three time zones."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I put savings into paying off the last of the car loan at 6 percent, or into the pension where the employer matches half?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I write the tests first here? It is a parser for a format I do not fully understand yet."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should the kids' bedtime story be the same one every night because they ask for it, or should I keep rotating?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I bring the whole presentation or just three slides and talk? It is a 20 minute internal update to people who already know the project."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we keep the monorepo or split the mobile app out? Mobile releases are monthly, backend is continuous."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I learn to make espresso properly or just buy a good filter setup? I drink two cups a day and do not love faff."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I apologise again for the thing last week, or is raising it a second time making it bigger than it was?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should the log line include the full payload or just the ids? It would help debugging a lot but some fields are personal data."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I take the promotion that is mostly management, or stay hands-on and ask for a senior track instead?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we buy a second-hand estate car or keep renting a van four weekends a year? We have one small car already."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I paint the north-facing room a warm white or lean into it with something dark? It gets almost no direct sun."""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I answer the interview question about my gap year honestly, or keep it short and move on?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should we freeze features for two weeks to fix the flaky test suite, or keep shipping and fix them as they bite?"""),
dict(cat="hn_should_i", lang="en", sys=None, prompt="""Should I use a bread machine for the daily loaf and keep the hand method for weekends, or is that just extra clutter?"""),
dict(cat="hn_should_i", lang="de", sys=None, prompt="""Sollte ich das alte Fahrrad reparieren lassen oder ein gebrauchtes kaufen? Die Reparatur kostet etwa ein Drittel eines guten gebrauchten Rads."""),
dict(cat="hn_should_i", lang="de", sys=None, prompt="""Sollen wir das Projekt intern weiterfuehren oder an eine Agentur geben? Wir haben Wissen im Haus, aber keine Zeit."""),
dict(cat="hn_should_i", lang="fr", sys=None, prompt="""Est-ce que je devrais changer de poste maintenant ou attendre la fin du projet dans six mois? L'equipe est bonne mais je m'ennuie."""),
dict(cat="hn_should_i", lang="es", sys=None, prompt="""Deberia matricularme en el curso intensivo de verano o repartirlo durante todo el ano? Trabajo a jornada completa."""),
dict(cat="hn_should_i", lang="it", sys=None, prompt="""Conviene riparare la lavatrice di dieci anni o comprarne una nuova? La riparazione costa circa duecento euro."""),
dict(cat="hn_should_i", lang="ja", sys=None, prompt="""引っ越しを春まで待つべきか、今の割安な物件を押さえるべきか迷っています。通勤時間は十分長くなります。"""),
dict(cat="hn_should_i", lang="pt", sys=None, prompt="""Devo terminar o curso de noite ou trocar de emprego primeiro e retomar depois? Faltam tres semestres."""),
dict(cat="hn_should_i", lang="nl", sys=None, prompt="""Moet ik de zolder isoleren of eerst de kozijnen vervangen? Het budget is er maar voor een van de twee."""),
dict(cat="hn_should_i", lang="ko", sys=None, prompt="""지금 노트북을 수리할까요, 아니면 새로 살까요? 수리비가 새 제품 가격의 40퍼센트 정도입니다."""),
dict(cat="hn_should_i", lang="is", sys=None, prompt="""Aetti eg ad kaupa nagladekk eda halda mig vid heilsarsdekk? Eg keyri adallega i borginni en fer austur um helgar."""),

# --- tool / function calling system prompts with JSON schemas ---
dict(cat="hn_tool_schema", lang="en",
     sys="""You are an assistant with access to tools. Available tools: {"name": "get_weather", "parameters": {"type": "object", "properties": {"city": {"type": "string"}, "days": {"type": "integer"}}, "required": ["city"]}}. Reply with a tool call or a normal answer.""",
     prompt="""What is the forecast in Reykjavik for the next three days?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Tools: search_orders(customer_id: string, status?: "open"|"shipped"|"cancelled") -> Order[]. Emit exactly one JSON object with keys "tool" and "args" when a tool is needed.""",
     prompt="""Find the open orders for customer c_88213."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You can call: {"name": "create_calendar_event", "input_schema": {"type": "object", "properties": {"title": {"type": "string"}, "start": {"type": "string", "format": "date-time"}, "minutes": {"type": "integer"}}, "required": ["title", "start"]}}""",
     prompt="""Put a 45 minute design review in my calendar for Thursday at 10."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Available function: convert_currency(amount: number, from: string, to: string). Respond only with the function call in JSON, nothing else.""",
     prompt="""How much is 240 euros in Icelandic krona?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You are a routing agent. Tools: {"lookup_policy": {"query": "string"}}, {"escalate": {"reason": "string", "priority": "low|normal|high"}}. Use at most one tool per turn.""",
     prompt="""A customer says their refund has not arrived after 14 days and they are threatening a chargeback."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Tool definitions: [{"name": "sql_query", "description": "Run a read-only SQL query", "parameters": {"type": "object", "properties": {"sql": {"type": "string"}}}}]""",
     prompt="""How many orders were placed last week, broken down by country?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You have one tool: send_email(to: string[], subject: string, body: string). Never send without confirming the recipient list first.""",
     prompt="""Email the design team that the review moved to Friday."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Function schema: {"name": "track_shipment", "parameters": {"type": "object", "properties": {"carrier": {"enum": ["dhl", "ups", "posturinn"]}, "tracking_number": {"type": "string"}}, "required": ["carrier", "tracking_number"]}}""",
     prompt="""Where is my parcel? It went with DHL, number 7712 4490 331."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You are a home assistant. Tools: set_thermostat(room: string, celsius: number), list_rooms(). If the room is ambiguous, ask.""",
     prompt="""Make it a bit warmer upstairs before we get home."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Tools available to you: {"name": "file_search", "parameters": {"query": "string", "max_results": "integer"}} and {"name": "read_file", "parameters": {"path": "string"}}.""",
     prompt="""Find where we define the retry backoff in this repo and show me the function."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You may call get_stock(sku: string, warehouse?: string) and reserve_stock(sku: string, qty: integer). Confirm quantities with the user before reserving.""",
     prompt="""Do we still have twelve of SKU AB-1192 in the Keflavik warehouse?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Toolset: {"translate": {"text": "string", "target_lang": "string"}}, {"detect_language": {"text": "string"}}. Prefer detect_language when the source is unclear.""",
     prompt="""What does 'Þetta reddast' mean and where would someone say it?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Agent tools: book_table(restaurant_id: string, party_size: integer, time: string), search_restaurants(area: string, cuisine?: string). Always search before booking.""",
     prompt="""Somewhere for six people near the harbour on Saturday evening, ideally fish."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You expose one function: summarise_document({"path": "string", "style": {"enum": ["bullets", "paragraph"]}}). Ask for the path if it is missing.""",
     prompt="""Give me the bullet version of the quarterly report."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Tools: run_tests(suite?: string), read_logs(service: string, since: string). Output a JSON tool call or plain text, never both.""",
     prompt="""The checkout service started erroring around nine this morning. Have a look."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Available actions: {"name": "geocode", "parameters": {"type": "object", "properties": {"address": {"type": "string"}}}}, {"name": "route", "parameters": {"type": "object", "properties": {"from": {"type": "string"}, "to": {"type": "string"}, "mode": {"enum": ["car", "bike", "foot"]}}}}""",
     prompt="""How long would it take to cycle from Grandi to the university?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You are an internal helpdesk bot with tools: reset_password(user: string), unlock_account(user: string), check_mfa(user: string). Verify identity first by asking for the employee number.""",
     prompt="""I am locked out again, same as last Tuesday."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Tool: create_ticket({"type": "object", "properties": {"title": {"type": "string"}, "severity": {"enum": ["s1","s2","s3"]}, "component": {"type": "string"}}, "required": ["title", "severity"]})""",
     prompt="""Raise something for the intermittent 504s on the images endpoint, it is not customer facing yet."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You can use web_search(query: string) and fetch_page(url: string). Cite the pages you actually read.""",
     prompt="""What changed in the latest release of the library we use for PDF generation?"""),
dict(cat="hn_tool_schema", lang="en",
     sys="""Function list: [{"name": "add_expense", "parameters": {"amount": "number", "currency": "string", "category": "string", "note": "string"}}]. Category must be one of travel, food, hardware, other.""",
     prompt="""Log the 3,400 krona taxi from the airport yesterday."""),
dict(cat="hn_tool_schema", lang="de", sys="""Verfuegbare Werkzeuge: {"name": "kalender_suchen", "parameter": {"von": "string", "bis": "string"}}. Antworte mit einem Werkzeugaufruf als JSON oder mit normalem Text.""",
     prompt="""Wann habe ich naechste Woche zwei freie Stunden am Stueck?"""),
dict(cat="hn_tool_schema", lang="fr", sys="""Outils disponibles: rechercher_client(nom: string), historique_commandes(client_id: string). Un seul appel par tour.""",
     prompt="""Retrouve les commandes de madame Lefevre depuis janvier."""),
dict(cat="hn_tool_schema", lang="es", sys="""Herramientas: {"name": "consultar_factura", "parameters": {"type": "object", "properties": {"numero": {"type": "string"}}}}. Devuelve solo la llamada en JSON.""",
     prompt="""Necesito ver la factura numero 2026-0118."""),
dict(cat="hn_tool_schema", lang="ja", sys="""利用できるツール: search_docs(query: string), open_doc(id: string)。必要なときだけツールを呼び出してください。""",
     prompt="""去年の出張規程がどこにあるか探してください。"""),
dict(cat="hn_tool_schema", lang="pt", sys="""Ferramentas: {"nome": "consultar_estoque", "parametros": {"sku": "string"}}. Responda apenas com a chamada em JSON quando precisar.""",
     prompt="""Ainda temos o modelo XR-4 em estoque?"""),
dict(cat="hn_tool_schema", lang="nl", sys="""Beschikbare functies: zoek_klant(naam: string), maak_notitie(klant_id: string, tekst: string).""",
     prompt="""Zet bij De Vries een notitie dat hij volgende week teruggebeld wil worden."""),
dict(cat="hn_tool_schema", lang="zh", sys="""可用工具：{"name": "query_orders", "parameters": {"user_id": "string", "status": "string"}}。需要时只输出 JSON 调用。""",
     prompt="""帮我查一下用户 u_7781 还有哪些未付款的订单。"""),
dict(cat="hn_tool_schema", lang="ko", sys="""사용 가능한 도구: get_weather(city: string), get_air_quality(city: string). 필요한 경우에만 호출하세요.""",
     prompt="""내일 서울 날씨랑 미세먼지 어떤지 알려 주세요."""),
dict(cat="hn_tool_schema", lang="it", sys="""Strumenti: {"name": "cerca_documento", "parameters": {"query": "string", "anno": "integer"}}. Usa uno strumento per volta.""",
     prompt="""Trovami il contratto di manutenzione firmato nel 2024."""),
dict(cat="hn_tool_schema", lang="en",
     sys="""You are given tools as OpenAPI operations: GET /users/{id}, GET /users/{id}/invoices?status=, POST /invoices/{id}/void. Never void without an explicit confirmation.""",
     prompt="""Check whether user 4471 has any unpaid invoices from last quarter."""),
]
